# Where the PocketBase server is reachable. Defaults to http://127.0.0.1:8090.
# NEXT_PUBLIC_* is inlined at build time, so rebuild after changing it.
NEXT_PUBLIC_POCKETBASE_URL=http://127.0.0.1:8090
